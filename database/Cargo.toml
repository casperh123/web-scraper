[package]
name = "database"
version = "0.1.0"
edition = "2024"

[dependencies]
sea-orm = { version = "2.0.0-rc", features = ["schema-sync", "entity-registry", "sqlx-postgres", "runtime-tokio-rustls"] }
tokio = {
    version = "1",
    features = ["sync"]
}

